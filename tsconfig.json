{
  "compilerOptions": {
    "target": "ES2022",
    "module": "ES2022",
    "moduleResolution": "bundler",
    "esModuleInterop": true,
    "forceConsistentCasingInFileNames": true,
    "strict": true,
    "skipLibCheck": true,
    "resolveJsonModule": true,
    "outDir": "./dist",
    "rootDir": ".",
    "allowImportingTsExtensions": false
  },
  "include": [
    "./src/**/*.ts",
    "./build/*/contract/*.d.ts"
  ],
  "//": "scripts/deploy-preprod.ts (gated testnet path) is run via tsx, not tsc-gated. It carries known midnight-js 4.1.1 API drift (LevelPrivateStateProvider config, deployContract/findDeployedContract signatures, WalletFacade.close) to be migrated together with the gated preprod deploy — see README 'Follow-ups'."
}
